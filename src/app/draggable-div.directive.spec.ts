import { DraggableDivDirective } from './draggable-div.directive';

describe('DraggableDivDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableDivDirective();
    expect(directive).toBeTruthy();
  });
});
